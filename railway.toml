[build]
builder = "NIXPACKS"
buildCommand = "mvn -DskipTests clean package"

[deploy]
startCommand = "sh -c 'java -Dserver.port=$PORT -jar $(ls target/*.jar | grep -v \\.original | head -n 1)'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

